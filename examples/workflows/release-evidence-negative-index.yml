name: Audit Shipguard release evidence negative fixtures

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  negative-evidence:
    runs-on: ubuntu-latest
    steps:
      - name: Audit bundled release evidence negative fixtures
        uses: jlekerli-source/shipguard/actions/release-evidence-negative-index@v3.38.0
        with:
          mode: fail
          artifact-name: shipguard-release-evidence-negative-index
