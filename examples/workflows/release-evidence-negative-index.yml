name: Audit Codex maintainer release evidence negative fixtures

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  negative-evidence:
    runs-on: ubuntu-latest
    steps:
      - name: Audit bundled release evidence negative fixtures
        uses: jlekerli-source/ringly-codex-workflows/actions/release-evidence-negative-index@v3.39.0
        with:
          mode: fail
          artifact-name: codex-maintainer-release-evidence-negative-index
