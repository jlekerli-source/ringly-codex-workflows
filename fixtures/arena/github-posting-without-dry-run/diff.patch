diff --git a/.github/workflows/release-post.yml b/.github/workflows/release-post.yml
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/.github/workflows/release-post.yml
@@ -0,0 +1,11 @@
+name: Release Post
+permissions: write-all
+on:
+  workflow_dispatch:
+jobs:
+  post:
+    runs-on: ubuntu-latest
+    steps:
+      - run: |
+          dry_run=false curl -fsS -X POST "https://api.github.com/repos/$GITHUB_REPOSITORY/releases" \
+            -H "authorization: Bearer $GITHUB_TOKEN" --data @release.json
