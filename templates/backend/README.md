# Backend Profile

Use this profile for API services, workers, queues, and server-side applications.

```bash
shipguard init backend ../my-service
shipguard doctor backend ../my-service
```

Customize `AGENTS.md` with real service names, test commands, migration rules, protected production paths, and rollout expectations before using it for implementation work.
