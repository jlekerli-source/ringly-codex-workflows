# CLI Profile

Use this profile for command-line tools, developer utilities, release helpers, and automation scripts.

```bash
shipguard init cli ../my-tool
shipguard doctor cli ../my-tool
```

Customize `AGENTS.md` with real commands, supported platforms, output contracts, packaging checks, and compatibility rules before using it for implementation work.
